# =============================================================================
# Legacy alias — kept for back-compat with older muscle memory.
# The canonical name is now `run_all_top.do`. Both do the same thing.
#
# Usage (from sim/ directory):
#   do run_top_tests.do      ;# legacy
#   do run_all_top.do        ;# canonical, recommended
# =============================================================================

do run_all_top.do

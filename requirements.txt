# Python dependencies for Dual-FPGA Trading Engine software
#
# On the PYNQ board (Board B), pynq is preinstalled — only Flask is needed:
#   pip install flask
#
# On a laptop for mock-mode development:
#   pip install -r requirements.txt

flask>=2.3
